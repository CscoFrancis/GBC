export function SectionLabel({ children }: { children: string }) {
  return (
    <span className="mb-3 block text-[0.72rem] font-semibold tracking-[0.22em] text-green uppercase">
      {children}
    </span>
  );
}
